% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/5-loo.r
\name{loo_modgirt}
\alias{loo_modgirt}
\title{Approximate leave-one-out cross-validation for a MODGIRT model}
\usage{
loo_modgirt(x, group = "dyad", stan_data = NULL, drop_nonfinite = FALSE, ...)
}
\arguments{
\item{x}{A fitted model from \code{\link[=fit_modgirt]{fit_modgirt()}}, fit with
\code{gen_log_lik = TRUE}.}

\item{group}{(string) Level at which to sum log-likelihoods. One of
\code{"dyad"} (the default; group-by-item, i.e. all periods of one item for
one group), \code{"observation"}, \code{"unit"}, \code{"period"}, or \code{"item"}.}

\item{stan_data}{(list or \code{NULL}) The data the model was fit to. By default
taken from \code{x}.}

\item{drop_nonfinite}{(logical) If \code{log_lik} contains non-finite values,
should the affected iterations be discarded rather than triggering an
error? Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[loo:loo]{loo::loo()}}.}
}
\value{
A \code{loo} object.
}
\description{
Computes PSIS-LOO for a fitted group-level IRT model, using the \pkg{loo}
package.
}
\details{
The finest unit of the MODGIRT likelihood is a group-item-period cell: the
responses of one demographic group to one item in one period, aggregated
into counts. Holding out one such cell therefore removes every respondent
who answered that item in that group and period, so this is
leave-a-block-of-respondents-out rather than leave-one-respondent-out. Its
\code{elpd_loo} is not on the same scale as that of \code{\link[=loo_mixfac]{loo_mixfac()}} and the two
cannot be compared.

When \code{shape_modgirt()} was given \code{weight_var}, the counts in \code{SSSS} are
weighted and generally non-integer, so the quantity being cross-validated
is a pseudo-likelihood. The Pareto k diagnostics remain informative about
the reliability of the importance sampling, but the implied effective
sample sizes should be read loosely.

As with \code{\link[=loo_mixfac]{loo_mixfac()}}, \code{group = "unit"} holds out a group's entire
response history, creating folds over which importance sampling is
unreliable; prefer K-fold cross-validation in that case. Models compared
with \code{\link[loo:loo_compare]{loo::loo_compare()}} must be fit to identical data and grouped
identically.
}
\examples{
\dontrun{
f1 <- fit_modgirt(stan_data, n_factor = 1, gen_log_lik = TRUE)
f2 <- fit_modgirt(stan_data, n_factor = 2, gen_log_lik = TRUE)
loo::loo_compare(list(d1 = loo_modgirt(f1), d2 = loo_modgirt(f2)))
}

}
\seealso{
\code{\link[=loo_mixfac]{loo_mixfac()}}, \code{\link[=log_lik_index_modgirt]{log_lik_index_modgirt()}}, \code{\link[=loo_influential]{loo_influential()}}
}
