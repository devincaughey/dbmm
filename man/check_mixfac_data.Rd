% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/1-prepare_data.r
\name{check_mixfac_data}
\alias{check_mixfac_data}
\title{Check long-format data for problems affecting a mixed factor model}
\usage{
check_mixfac_data(
  long_data,
  unit_var,
  time_var,
  item_var,
  value_var,
  periods_to_estimate = NULL,
  binary_items = NA,
  trichotomous_items = NA,
  ordinal_items = NA,
  max_cats = 5,
  min_cat_n = 3,
  quiet = FALSE
)
}
\arguments{
\item{long_data}{(data frame) Data in long (unit-period-item) form.}

\item{unit_var, time_var, item_var, value_var}{(strings) Column names, as for
\code{\link[=shape_mixfac]{shape_mixfac()}}.}

\item{periods_to_estimate}{(vector or \code{NULL}) Periods to retain before
checking. \code{NULL}, the default, uses every period present.}

\item{binary_items, trichotomous_items, ordinal_items}{(character vectors)
Items of each type. \code{NA}, the default, classifies items by their number
of distinct values, as \code{\link[=shape_mixfac]{shape_mixfac()}} does.}

\item{max_cats}{(positive integer) Largest number of distinct values for
which an item is classified as ordinal. Defaults to \code{5}.}

\item{min_cat_n}{(non-negative integer) Warn when a trichotomous or ordinal
item has a response category with fewer than this many observations.
Cutpoints bounding sparse categories are weakly identified. \code{0}
disables the check. Defaults to \code{3}.}

\item{quiet}{(logical) Suppress the informational summary? Warnings are
emitted regardless.}
}
\value{
Invisibly, a list of data frames: \code{duplicates}, \code{no_variation},
\code{single_period}, \code{sparse_categories}, \code{missing_levels}, and
\code{recoded_binary}, each with zero rows if that check found nothing.
}
\description{
Reports data problems that \code{\link[=shape_mixfac]{shape_mixfac()}} would otherwise pass over
silently, or handle in ways that may surprise. It is called by
\code{\link[=shape_mixfac]{shape_mixfac()}}, and can also be run beforehand to inspect data.
}
\details{
The checks are:
\itemize{
\item \strong{Duplicated observations.} \code{\link[=shape_mixfac]{shape_mixfac()}} takes the data at face
value, so a repeated unit-period-item combination enters the likelihood
more than once.
\item \strong{Items with no variation}, which \code{\link[=shape_mixfac]{shape_mixfac()}} drops.
\item \strong{Items observed in a single period}, which inform the loadings but
contribute nothing to the estimated dynamics.
\item \strong{Sparse response categories} in trichotomous and ordinal items.
\item \strong{Missing intermediate levels.} \code{\link[=shape_mixfac]{shape_mixfac()}} recodes ordered
responses with \code{as.integer(ordered(value))}, which numbers the observed
values consecutively. An item coded 1, 2, 4, 5 therefore becomes a
four-category item, and its second cutpoint separates the original values
2 and 4 rather than 2 and 3.
\item \strong{Binary items not coded 0/1}, which are recoded to 0 and 1 in the
order of their values.
}
}
\examples{
data("state_policies_2010_2012")
invisible(check_mixfac_data(
    subset(state_policies_2010_2012, !is.na(value_real)),
    unit_var = "state_abb", time_var = "year",
    item_var = "policy_variable", value_var = "value_real",
    min_cat_n = 0, quiet = TRUE
))

}
